import React from 'react';

export default function Contact() {
  return(
    <section className="contact" id="contact">
      <h1>Contact</h1>
      <h3>When a kid holds a balloon he prevents it from floating awaying, but</h3>
    </section>
  );
}